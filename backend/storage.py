# storage.py
from farm import Farm

def load_user_farm(user_id: str | None):
    """
    Load farm for a user.
    Later: DB / Redis / file.
    Now: always return None (new user).
    """
    return None
